# Expo / React Native's Metro bundler does not understand pnpm's symlinked
# node_modules layout. Hoisting keeps the mobile app working.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
